---
apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  labels:
    name: "{{ .Release.Name }}"
spec:
  type: {{ .Values.service.spec.type }}
  selector:
    app: "{{ .Release.Name }}"
  ports:
    - port: {{ .Values.service.port }}
